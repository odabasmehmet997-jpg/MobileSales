package com.fasterxml.jackson.databind.node;

import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectReader;
import com.fasterxml.jackson.databind.ObjectWriter;
import com.fasterxml.jackson.databind.json.JsonMapper;
import java.io.IOException;

// Class generated by  decompiler (1.5.3)
/* loaded from: D:\Android\sil\Yeni klasör (2)\classes.dex */
final class InternalNodeMapper {
    private static final JsonMapper JSON_MAPPER;
    private static final ObjectReader NODE_READER;
    private static final ObjectWriter PRETTY_WRITER;
    private static final ObjectWriter STD_WRITER;

    InternalNodeMapper() {
    }

    static {
        final JsonMapper jsonMapper = new JsonMapper();
        JSON_MAPPER = jsonMapper;
        STD_WRITER = jsonMapper.writer();
        PRETTY_WRITER = jsonMapper.writer().withDefaultPrettyPrinter();
        NODE_READER = jsonMapper.readerFor(JsonNode.class);
    }

    public static String nodeToString(final JsonNode jsonNode) {
        try {
            return InternalNodeMapper.STD_WRITER.writeValueAsString(jsonNode);
        } catch (final IOException e2) {
            throw new RuntimeException(e2);
        }
    }

    public static String nodeToPrettyString(final JsonNode jsonNode) {
        try {
            return InternalNodeMapper.PRETTY_WRITER.writeValueAsString(jsonNode);
        } catch (final IOException e2) {
            throw new RuntimeException(e2);
        }
    }

    public static byte[] valueToBytes(final Object obj) throws IOException {
        return InternalNodeMapper.JSON_MAPPER.writeValueAsBytes(obj);
    }

    public static JsonNode bytesToNode(final byte[] bArr) throws IOException {
        return InternalNodeMapper.NODE_READER.readValue(bArr);
    }
}
