package com.fasterxml.jackson.databind.jsontype.impl;

import com.fasterxml.jackson.databind.DatabindContext;
import com.fasterxml.jackson.databind.JavaType;
import com.fasterxml.jackson.databind.cfg.MapperConfig;
import com.fasterxml.jackson.databind.jsontype.PolymorphicTypeValidator;
import com.fasterxml.jackson.databind.type.TypeFactory;
import java.io.IOException;

// Class generated by  decompiler (1.5.3)
/* loaded from: D:\Android\sil\Yeni klasör (2)\classes.dex */
public class MinimalClassNameIdResolver extends ClassNameIdResolver {
    protected final String _basePackageName;
    protected final String _basePackagePrefix;

    protected MinimalClassNameIdResolver(final JavaType javaType, final TypeFactory typeFactory, final PolymorphicTypeValidator polymorphicTypeValidator) {
        super(javaType, typeFactory, polymorphicTypeValidator);
        final String name = javaType.getRawClass().getName();
        final int iLastIndexOf = name.lastIndexOf(46);
        if (0 > iLastIndexOf) {
            _basePackageName = "";
            _basePackagePrefix = ".";
        } else {
            _basePackagePrefix = name.substring(0, iLastIndexOf + 1);
            _basePackageName = name.substring(0, iLastIndexOf);
        }
    }

    public static MinimalClassNameIdResolver construct(final JavaType javaType, final MapperConfig<?> mapperConfig, final PolymorphicTypeValidator polymorphicTypeValidator) {
        return new MinimalClassNameIdResolver(javaType, mapperConfig.getTypeFactory(), polymorphicTypeValidator);
    }

    @Override // com.fasterxml.jackson.databind.jsontype.impl.ClassNameIdResolver, com.fasterxml.jackson.databind.jsontype.TypeIdResolver
    public String idFromValue(final Object obj) {
        final String name = obj.getClass().getName();
        return name.startsWith(_basePackagePrefix) ? name.substring(_basePackagePrefix.length() - 1) : name;
    }

    @Override // com.fasterxml.jackson.databind.jsontype.impl.ClassNameIdResolver
    protected JavaType _typeFromId(String str, final DatabindContext databindContext) throws IOException {
        if (str.startsWith(".")) {
            final StringBuilder sb = new StringBuilder(str.length() + _basePackageName.length());
            if (_basePackageName.isEmpty()) {
                sb.append(str.substring(1));
            } else {
                sb.append(_basePackageName);
                sb.append(str);
            }
            str = sb.toString();
        }
        return super._typeFromId(str, databindContext);
    }
}
