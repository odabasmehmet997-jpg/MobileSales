package com.fasterxml.jackson.databind.ser;

import com.fasterxml.jackson.annotation.*;
import com.fasterxml.jackson.databind.*;
import com.fasterxml.jackson.databind.cfg.SerializerFactoryConfig;
import com.fasterxml.jackson.databind.introspect.*;
import com.fasterxml.jackson.databind.jsontype.TypeResolverBuilder;
import com.fasterxml.jackson.databind.jsontype.TypeSerializer;
import com.fasterxml.jackson.databind.ser.impl.FilteredBeanPropertyWriter;
import com.fasterxml.jackson.databind.ser.impl.ObjectIdWriter;
import com.fasterxml.jackson.databind.ser.impl.PropertyBasedObjectIdGenerator;
import com.fasterxml.jackson.databind.ser.impl.UnsupportedTypeSerializer;
import com.fasterxml.jackson.databind.ser.std.MapSerializer;
import com.fasterxml.jackson.databind.ser.std.StdDelegatingSerializer;
import com.fasterxml.jackson.databind.type.ReferenceType;
import com.fasterxml.jackson.databind.util.BeanUtil;
import com.fasterxml.jackson.databind.util.ClassUtil;
import com.fasterxml.jackson.databind.util.Converter;
import com.fasterxml.jackson.databind.util.IgnorePropertiesUtil;

import java.util.*;

// Class generated by  decompiler (1.5.3)
/* loaded from: D:\Android\sil\Yeni klasör (2)\classes.dex */
public class BeanSerializerFactory extends BasicSerializerFactory {
    public static final BeanSerializerFactory instance = new BeanSerializerFactory(null);
    private static final long serialVersionUID = 1;

    protected BeanSerializerFactory(final SerializerFactoryConfig serializerFactoryConfig) {
        super(serializerFactoryConfig);
    }

    @Override // com.fasterxml.jackson.databind.ser.BasicSerializerFactory
    public SerializerFactory withConfig(final SerializerFactoryConfig serializerFactoryConfig) {
        if (_factoryConfig == serializerFactoryConfig) {
            return this;
        }
        if (BeanSerializerFactory.class != getClass()) {
            throw new IllegalStateException("Subtype of BeanSerializerFactory (" + this.getClass().getName() + ") has not properly overridden method 'withAdditionalSerializers': cannot instantiate subtype with additional serializer definitions");
        }
        return new BeanSerializerFactory(serializerFactoryConfig);
    }

    @Override // com.fasterxml.jackson.databind.ser.BasicSerializerFactory
    protected Iterable<Serializers> customSerializers() {
        return _factoryConfig.serializers();
    }

    @Override // com.fasterxml.jackson.databind.ser.BasicSerializerFactory, com.fasterxml.jackson.databind.ser.SerializerFactory
    public JsonSerializer<Object> createSerializer(final SerializerProvider serializerProvider, final JavaType javaType) throws JsonMappingException {
        final JavaType javaTypeRefineSerializationType;
        final SerializationConfig config = serializerProvider.getConfig();
        BeanDescription beanDescriptionIntrospect = config.introspect(javaType);
        JsonSerializer<?> jsonSerializerFindSerializerFromAnnotation = this.findSerializerFromAnnotation(serializerProvider, beanDescriptionIntrospect.getClassInfo());
        if (null != jsonSerializerFindSerializerFromAnnotation) {
            return jsonSerializerFindSerializerFromAnnotation;
        }
        final AnnotationIntrospector annotationIntrospector = config.getAnnotationIntrospector();
        boolean z = false;
        if (null == annotationIntrospector) {
            javaTypeRefineSerializationType = javaType;
        } else {
            try {
                javaTypeRefineSerializationType = annotationIntrospector.refineSerializationType(config, beanDescriptionIntrospect.getClassInfo(), javaType);
            } catch (final JsonMappingException e2) {
                return serializerProvider.reportBadTypeDefinition(beanDescriptionIntrospect, e2.getMessage(), new Object[0]);
            }
        }
        if (javaTypeRefineSerializationType != javaType) {
            if (!javaTypeRefineSerializationType.hasRawClass(javaType.getRawClass())) {
                beanDescriptionIntrospect = config.introspect(javaTypeRefineSerializationType);
            }
            z = true;
        }
        final Converter<Object, Object> converterFindSerializationConverter = beanDescriptionIntrospect.findSerializationConverter();
        if (null == converterFindSerializationConverter) {
            return this._createSerializer2(serializerProvider, javaTypeRefineSerializationType, beanDescriptionIntrospect, z);
        }
        final JavaType outputType = converterFindSerializationConverter.getOutputType(serializerProvider.getTypeFactory());
        if (!outputType.hasRawClass(javaTypeRefineSerializationType.getRawClass())) {
            beanDescriptionIntrospect = config.introspect(outputType);
            jsonSerializerFindSerializerFromAnnotation = this.findSerializerFromAnnotation(serializerProvider, beanDescriptionIntrospect.getClassInfo());
        }
        if (null == jsonSerializerFindSerializerFromAnnotation && !outputType.isJavaLangObject()) {
            jsonSerializerFindSerializerFromAnnotation = this._createSerializer2(serializerProvider, outputType, beanDescriptionIntrospect, true);
        }
        return new StdDelegatingSerializer(converterFindSerializationConverter, outputType, jsonSerializerFindSerializerFromAnnotation);
    }

    protected JsonSerializer<?> _createSerializer2(final SerializerProvider serializerProvider, final JavaType javaType, final BeanDescription beanDescription, boolean z) throws JsonMappingException {
        JsonSerializer<?> jsonSerializerFindSerializerByAnnotations;
        final SerializationConfig config = serializerProvider.getConfig();
        JsonSerializer<?> jsonSerializerFindSerializer = null;
        if (javaType.isContainerType()) {
            if (!z) {
                z = this.usesStaticTyping(config, beanDescription, null);
            }
            jsonSerializerFindSerializerByAnnotations = this.buildContainerSerializer(serializerProvider, javaType, beanDescription, z);
            if (null != jsonSerializerFindSerializerByAnnotations) {
                return jsonSerializerFindSerializerByAnnotations;
            }
        } else {
            if (javaType.isReferenceType()) {
                jsonSerializerFindSerializerByAnnotations = this.findReferenceSerializer(serializerProvider, (ReferenceType) javaType, beanDescription, z);
            } else {
                final Iterator<Serializers> it = this.customSerializers().iterator();
                while (it.hasNext() && null == (jsonSerializerFindSerializer = it.next().findSerializer(config, javaType, beanDescription))) {
                }
                jsonSerializerFindSerializerByAnnotations = jsonSerializerFindSerializer;
            }
            if (null == jsonSerializerFindSerializerByAnnotations) {
                jsonSerializerFindSerializerByAnnotations = this.findSerializerByAnnotations(serializerProvider, javaType, beanDescription);
            }
        }
        if (null == jsonSerializerFindSerializerByAnnotations && null == (jsonSerializerFindSerializerByAnnotations = findSerializerByLookup(javaType, config, beanDescription, z)) && null == (jsonSerializerFindSerializerByAnnotations = findSerializerByPrimaryType(serializerProvider, javaType, beanDescription, z)) && null == (jsonSerializerFindSerializerByAnnotations = findBeanOrAddOnSerializer(serializerProvider, javaType, beanDescription, z))) {
            jsonSerializerFindSerializerByAnnotations = serializerProvider.getUnknownTypeSerializer(beanDescription.getBeanClass());
        }
        if (null != jsonSerializerFindSerializerByAnnotations && _factoryConfig.hasSerializerModifiers()) {
            final Iterator<BeanSerializerModifier> it2 = _factoryConfig.serializerModifiers().iterator();
            while (it2.hasNext()) {
                jsonSerializerFindSerializerByAnnotations = it2.next().modifySerializer(config, beanDescription, jsonSerializerFindSerializerByAnnotations);
            }
        }
        return jsonSerializerFindSerializerByAnnotations;
    }

    @Deprecated
    public JsonSerializer<Object> findBeanSerializer(final SerializerProvider serializerProvider, final JavaType javaType, final BeanDescription beanDescription) throws JsonMappingException {
        return this.findBeanOrAddOnSerializer(serializerProvider, javaType, beanDescription, serializerProvider.isEnabled(MapperFeature.USE_STATIC_TYPING));
    }

    public JsonSerializer<Object> findBeanOrAddOnSerializer(final SerializerProvider serializerProvider, final JavaType javaType, final BeanDescription beanDescription, final boolean z) throws JsonMappingException {
        if (this.isPotentialBeanType(javaType.getRawClass()) || ClassUtil.isEnumType(javaType.getRawClass())) {
            return this.constructBeanOrAddOnSerializer(serializerProvider, javaType, beanDescription, z);
        }
        return null;
    }

    public TypeSerializer findPropertyTypeSerializer(final JavaType javaType, final SerializationConfig serializationConfig, final AnnotatedMember annotatedMember) throws JsonMappingException {
        final TypeResolverBuilder<?> typeResolverBuilderFindPropertyTypeResolver = serializationConfig.getAnnotationIntrospector().findPropertyTypeResolver(serializationConfig, annotatedMember, javaType);
        if (null == typeResolverBuilderFindPropertyTypeResolver) {
            return this.createTypeSerializer(serializationConfig, javaType);
        }
        return typeResolverBuilderFindPropertyTypeResolver.buildTypeSerializer(serializationConfig, javaType, serializationConfig.getSubtypeResolver().collectAndResolveSubtypesByClass(serializationConfig, annotatedMember, javaType));
    }

    public TypeSerializer findPropertyContentTypeSerializer(final JavaType javaType, final SerializationConfig serializationConfig, final AnnotatedMember annotatedMember) throws JsonMappingException {
        final JavaType contentType = javaType.getContentType();
        final TypeResolverBuilder<?> typeResolverBuilderFindPropertyContentTypeResolver = serializationConfig.getAnnotationIntrospector().findPropertyContentTypeResolver(serializationConfig, annotatedMember, javaType);
        if (null == typeResolverBuilderFindPropertyContentTypeResolver) {
            return this.createTypeSerializer(serializationConfig, contentType);
        }
        return typeResolverBuilderFindPropertyContentTypeResolver.buildTypeSerializer(serializationConfig, contentType, serializationConfig.getSubtypeResolver().collectAndResolveSubtypesByClass(serializationConfig, annotatedMember, contentType));
    }

    @Deprecated
    protected JsonSerializer<Object> constructBeanSerializer(final SerializerProvider serializerProvider, final BeanDescription beanDescription) throws JsonMappingException {
        return this.constructBeanOrAddOnSerializer(serializerProvider, beanDescription.getType(), beanDescription, serializerProvider.isEnabled(MapperFeature.USE_STATIC_TYPING));
    }

    protected JsonSerializer<Object> constructBeanOrAddOnSerializer(final SerializerProvider serializerProvider, final JavaType javaType, final BeanDescription beanDescription, final boolean z) throws JsonMappingException {
        List<BeanPropertyWriter> listRemoveOverlappingTypeIds;
        if (Object.class == beanDescription.getBeanClass()) {
            return serializerProvider.getUnknownTypeSerializer(Object.class);
        }
        final JsonSerializer<?> jsonSerializer_findUnsupportedTypeSerializer = this._findUnsupportedTypeSerializer(serializerProvider, javaType, beanDescription);
        if (null != jsonSerializer_findUnsupportedTypeSerializer) {
            return jsonSerializer_findUnsupportedTypeSerializer;
        }
        final SerializationConfig config = serializerProvider.getConfig();
        BeanSerializerBuilder beanSerializerBuilderConstructBeanSerializerBuilder = this.constructBeanSerializerBuilder(beanDescription);
        beanSerializerBuilderConstructBeanSerializerBuilder.setConfig(config);
        final List<BeanPropertyWriter> listFindBeanProperties = this.findBeanProperties(serializerProvider, beanDescription, beanSerializerBuilderConstructBeanSerializerBuilder);
        if (null == listFindBeanProperties) {
            listRemoveOverlappingTypeIds = new ArrayList<>();
        } else {
            listRemoveOverlappingTypeIds = this.removeOverlappingTypeIds(serializerProvider, beanDescription, beanSerializerBuilderConstructBeanSerializerBuilder, listFindBeanProperties);
        }
        serializerProvider.getAnnotationIntrospector().findAndAddVirtualProperties(config, beanDescription.getClassInfo(), listRemoveOverlappingTypeIds);
        if (_factoryConfig.hasSerializerModifiers()) {
            final Iterator<BeanSerializerModifier> it = _factoryConfig.serializerModifiers().iterator();
            while (it.hasNext()) {
                listRemoveOverlappingTypeIds = it.next().changeProperties(config, beanDescription, listRemoveOverlappingTypeIds);
            }
        }
        List<BeanPropertyWriter> listFilterBeanProperties = this.filterBeanProperties(config, beanDescription, listRemoveOverlappingTypeIds);
        if (_factoryConfig.hasSerializerModifiers()) {
            final Iterator<BeanSerializerModifier> it2 = _factoryConfig.serializerModifiers().iterator();
            while (it2.hasNext()) {
                listFilterBeanProperties = it2.next().orderProperties(config, beanDescription, listFilterBeanProperties);
            }
        }
        beanSerializerBuilderConstructBeanSerializerBuilder.setObjectIdWriter(this.constructObjectIdHandler(serializerProvider, beanDescription, listFilterBeanProperties));
        beanSerializerBuilderConstructBeanSerializerBuilder.setProperties(listFilterBeanProperties);
        beanSerializerBuilderConstructBeanSerializerBuilder.setFilterId(this.findFilterId(config, beanDescription));
        final AnnotatedMember annotatedMemberFindAnyGetter = beanDescription.findAnyGetter();
        if (null != annotatedMemberFindAnyGetter) {
            final JavaType type = annotatedMemberFindAnyGetter.getType();
            final JavaType contentType = type.getContentType();
            final TypeSerializer typeSerializerCreateTypeSerializer = this.createTypeSerializer(config, contentType);
            JsonSerializer<Object> jsonSerializerFindSerializerFromAnnotation = this.findSerializerFromAnnotation(serializerProvider, annotatedMemberFindAnyGetter);
            if (null == jsonSerializerFindSerializerFromAnnotation) {
                jsonSerializerFindSerializerFromAnnotation = MapSerializer.construct((Set<String>) null, type, config.isEnabled(MapperFeature.USE_STATIC_TYPING), typeSerializerCreateTypeSerializer, (JsonSerializer<Object>) null, (JsonSerializer<Object>) null, (Object) null);
            }
            beanSerializerBuilderConstructBeanSerializerBuilder.setAnyGetter(new AnyGetterWriter(new BeanProperty.Std(PropertyName.construct(annotatedMemberFindAnyGetter.getName()), contentType, null, annotatedMemberFindAnyGetter, PropertyMetadata.STD_OPTIONAL), annotatedMemberFindAnyGetter, jsonSerializerFindSerializerFromAnnotation));
        }
        this.processViews(config, beanSerializerBuilderConstructBeanSerializerBuilder);
        if (_factoryConfig.hasSerializerModifiers()) {
            final Iterator<BeanSerializerModifier> it3 = _factoryConfig.serializerModifiers().iterator();
            while (it3.hasNext()) {
                beanSerializerBuilderConstructBeanSerializerBuilder = it3.next().updateBuilder(config, beanDescription, beanSerializerBuilderConstructBeanSerializerBuilder);
            }
        }
        try {
            JsonSerializer<?> jsonSerializerBuild = beanSerializerBuilderConstructBeanSerializerBuilder.build();
            if (null == jsonSerializerBuild) {
                if (javaType.isRecordType()) {
                    return beanSerializerBuilderConstructBeanSerializerBuilder.createDummy();
                }
                jsonSerializerBuild = this.findSerializerByAddonType(config, javaType, beanDescription, z);
                if (null == jsonSerializerBuild && beanDescription.hasKnownClassAnnotations()) {
                    return beanSerializerBuilderConstructBeanSerializerBuilder.createDummy();
                }
            }
            return jsonSerializerBuild;
        } catch (final RuntimeException e2) {
            return serializerProvider.reportBadTypeDefinition(beanDescription, "Failed to construct BeanSerializer for %s: (%s) %s", beanDescription.getType(), e2.getClass().getName(), e2.getMessage());
        }
    }

    protected ObjectIdWriter constructObjectIdHandler(final SerializerProvider serializerProvider, final BeanDescription beanDescription, final List<BeanPropertyWriter> list) throws JsonMappingException {
        final ObjectIdInfo objectIdInfo = beanDescription.getObjectIdInfo();
        if (null == objectIdInfo) {
            return null;
        }
        final Class<? extends ObjectIdGenerator<?>> generatorType = objectIdInfo.getGeneratorType();
        if (ObjectIdGeneratorsPropertyGenerator.class == generatorType) {
            final String simpleName = objectIdInfo.getPropertyName().getSimpleName();
            final int size = list.size();
            for (int i2 = 0; i2 != size; i2++) {
                final BeanPropertyWriter beanPropertyWriter = list.get(i2);
                if (simpleName.equals(beanPropertyWriter.getName())) {
                    if (0 < i2) {
                        list.remove(i2);
                        list.add(0, beanPropertyWriter);
                    }
                    return ObjectIdWriter.construct(beanPropertyWriter.getType(), null, new PropertyBasedObjectIdGenerator(objectIdInfo, beanPropertyWriter), objectIdInfo.getAlwaysAsId());
                }
            }
            throw new IllegalArgumentException(String.format("Invalid Object Id definition for %s: cannot find property with name %s", ClassUtil.getTypeDescription(beanDescription.getType()), ClassUtil.name(simpleName)));
        }
        return ObjectIdWriter.construct(serializerProvider.getTypeFactory().findTypeParameters(serializerProvider.constructType(generatorType), ObjectIdGenerator.class)[0], objectIdInfo.getPropertyName(), serializerProvider.objectIdGeneratorInstance(beanDescription.getClassInfo(), objectIdInfo), objectIdInfo.getAlwaysAsId());
    }

    protected BeanPropertyWriter constructFilteredBeanWriter(final BeanPropertyWriter beanPropertyWriter, final Class<?>[] clsArr) {
        return FilteredBeanPropertyWriter.constructViewBased(beanPropertyWriter, clsArr);
    }

    protected PropertyBuilder constructPropertyBuilder(final SerializationConfig serializationConfig, final BeanDescription beanDescription) {
        return new PropertyBuilder(serializationConfig, beanDescription);
    }

    protected BeanSerializerBuilder constructBeanSerializerBuilder(final BeanDescription beanDescription) {
        return new BeanSerializerBuilder(beanDescription);
    }

    protected boolean isPotentialBeanType(final Class<?> cls) {
        return null == ClassUtil.canBeABeanType(cls) && !ClassUtil.isProxyType(cls);
    }

    protected List<BeanPropertyWriter> findBeanProperties(final SerializerProvider serializerProvider, final BeanDescription beanDescription, final BeanSerializerBuilder beanSerializerBuilder) throws JsonMappingException {
        final List<BeanPropertyDefinition> listFindProperties = beanDescription.findProperties();
        final SerializationConfig config = serializerProvider.getConfig();
        this.removeIgnorableTypes(config, beanDescription, listFindProperties);
        if (config.isEnabled(MapperFeature.REQUIRE_SETTERS_FOR_GETTERS)) {
            this.removeSetterlessGetters(config, beanDescription, listFindProperties);
        }
        if (listFindProperties.isEmpty()) {
            return null;
        }
        final boolean zUsesStaticTyping = this.usesStaticTyping(config, beanDescription, null);
        final PropertyBuilder propertyBuilderConstructPropertyBuilder = this.constructPropertyBuilder(config, beanDescription);
        final ArrayList arrayList = new ArrayList(listFindProperties.size());
        for (final BeanPropertyDefinition beanPropertyDefinition : listFindProperties) {
            final AnnotatedMember accessor = beanPropertyDefinition.getAccessor();
            if (!beanPropertyDefinition.isTypeId()) {
                final AnnotationIntrospector.ReferenceProperty referencePropertyFindReferenceType = beanPropertyDefinition.findReferenceType();
                if (null == referencePropertyFindReferenceType || !referencePropertyFindReferenceType.isBackReference()) {
                    if (accessor instanceof AnnotatedMethod) {
                        arrayList.add(this._constructWriter(serializerProvider, beanPropertyDefinition, propertyBuilderConstructPropertyBuilder, zUsesStaticTyping, (AnnotatedMethod) accessor));
                    } else {
                        arrayList.add(this._constructWriter(serializerProvider, beanPropertyDefinition, propertyBuilderConstructPropertyBuilder, zUsesStaticTyping, (AnnotatedField) accessor));
                    }
                }
            } else if (null != accessor) {
                beanSerializerBuilder.setTypeId(accessor);
            }
        }
        return arrayList;
    }

    protected List<BeanPropertyWriter> filterBeanProperties(final SerializationConfig serializationConfig, final BeanDescription beanDescription, final List<BeanPropertyWriter> list) {
        final JsonIgnoreProperties.Value defaultPropertyIgnorals = serializationConfig.getDefaultPropertyIgnorals(beanDescription.getBeanClass(), beanDescription.getClassInfo());
        final Set<String> setFindIgnoredForSerialization = null != defaultPropertyIgnorals ? defaultPropertyIgnorals.findIgnoredForSerialization() : null;
        final JsonIncludeProperties.Value defaultPropertyInclusions = serializationConfig.getDefaultPropertyInclusions(beanDescription.getBeanClass(), beanDescription.getClassInfo());
        final Set<String> included = null != defaultPropertyInclusions ? defaultPropertyInclusions.getIncluded() : null;
        if (null != included || (null != setFindIgnoredForSerialization && !setFindIgnoredForSerialization.isEmpty())) {
            final Iterator<BeanPropertyWriter> it = list.iterator();
            while (it.hasNext()) {
                if (IgnorePropertiesUtil.shouldIgnore(it.next().getName(), setFindIgnoredForSerialization, included)) {
                    it.remove();
                }
            }
        }
        return list;
    }

    protected void processViews(final SerializationConfig serializationConfig, final BeanSerializerBuilder beanSerializerBuilder) {
        final List<BeanPropertyWriter> properties = beanSerializerBuilder.getProperties();
        final boolean zIsEnabled = serializationConfig.isEnabled(MapperFeature.DEFAULT_VIEW_INCLUSION);
        final int size = properties.size();
        final BeanPropertyWriter[] beanPropertyWriterArr = new BeanPropertyWriter[size];
        int i2 = 0;
        for (int i3 = 0; i3 < size; i3++) {
            final BeanPropertyWriter beanPropertyWriter = properties.get(i3);
            final Class<?>[] views = beanPropertyWriter.getViews();
            if (null != views && 0 != views.length) {
                i2++;
                beanPropertyWriterArr[i3] = this.constructFilteredBeanWriter(beanPropertyWriter, views);
            } else if (zIsEnabled) {
                beanPropertyWriterArr[i3] = beanPropertyWriter;
            }
        }
        if (zIsEnabled && 0 == i2) {
            return;
        }
        beanSerializerBuilder.setFilteredProperties(beanPropertyWriterArr);
    }

    protected void removeIgnorableTypes(final SerializationConfig serializationConfig, final BeanDescription beanDescription, final List<BeanPropertyDefinition> list) {
        final AnnotationIntrospector annotationIntrospector = serializationConfig.getAnnotationIntrospector();
        final HashMap map = new HashMap();
        final Iterator<BeanPropertyDefinition> it = list.iterator();
        while (it.hasNext()) {
            final BeanPropertyDefinition next = it.next();
            if (null == next.getAccessor()) {
                it.remove();
            } else {
                final Class<?> rawPrimaryType = next.getRawPrimaryType();
                Boolean isIgnoredType = (Boolean) map.get(rawPrimaryType);
                if (null == isIgnoredType) {
                    isIgnoredType = serializationConfig.getConfigOverride(rawPrimaryType).getIsIgnoredType();
                    if (null == isIgnoredType && null == (isIgnoredType = annotationIntrospector.isIgnorableType(serializationConfig.introspectClassAnnotations(rawPrimaryType).getClassInfo()))) {
                        isIgnoredType = Boolean.FALSE;
                    }
                    map.put(rawPrimaryType, isIgnoredType);
                }
                if (isIgnoredType.booleanValue()) {
                    it.remove();
                }
            }
        }
    }

    protected void removeSetterlessGetters(final SerializationConfig serializationConfig, final BeanDescription beanDescription, final List<BeanPropertyDefinition> list) {
        final Iterator<BeanPropertyDefinition> it = list.iterator();
        while (it.hasNext()) {
            final BeanPropertyDefinition next = it.next();
            if (!next.couldDeserialize() && !next.isExplicitlyIncluded()) {
                it.remove();
            }
        }
    }

    protected List<BeanPropertyWriter> removeOverlappingTypeIds(final SerializerProvider serializerProvider, final BeanDescription beanDescription, final BeanSerializerBuilder beanSerializerBuilder, final List<BeanPropertyWriter> list) {
        final int size = list.size();
        for (int i2 = 0; i2 < size; i2++) {
            final BeanPropertyWriter beanPropertyWriter = list.get(i2);
            final TypeSerializer typeSerializer = beanPropertyWriter.getTypeSerializer();
            if (null != typeSerializer && JsonTypeInfo.EnumC1184As.EXTERNAL_PROPERTY == typeSerializer.getTypeInclusion()) {
                final PropertyName propertyNameConstruct = PropertyName.construct(typeSerializer.getPropertyName());
                final Iterator<BeanPropertyWriter> it = list.iterator();
                while (true) {
                    if (!it.hasNext()) {
                        break;
                    }
                    final BeanPropertyWriter next = it.next();
                    if (next != beanPropertyWriter && next.wouldConflictWithName(propertyNameConstruct)) {
                        beanPropertyWriter.assignTypeSerializer(null);
                        break;
                    }
                }
            }
        }
        return list;
    }


    protected BeanPropertyWriter _constructWriter(final SerializerProvider serializerProvider, final BeanPropertyDefinition beanPropertyDefinition, final PropertyBuilder propertyBuilder, final boolean z, final AnnotatedMember annotatedMember) throws JsonMappingException {
        final PropertyName fullName = beanPropertyDefinition.getFullName();
        final JavaType type = annotatedMember.getType();
        final BeanProperty.Std std = new BeanProperty.Std(fullName, type, beanPropertyDefinition.getWrapperName(), annotatedMember, beanPropertyDefinition.getMetadata());
        final JsonSerializer<Object> jsonSerializerFindSerializerFromAnnotation = this.findSerializerFromAnnotation(serializerProvider, annotatedMember);
        if (jsonSerializerFindSerializerFromAnnotation instanceof ResolvableSerializer) {
            ((ResolvableSerializer) jsonSerializerFindSerializerFromAnnotation).resolve(serializerProvider);
        }
        return propertyBuilder.buildWriter(serializerProvider, beanPropertyDefinition, type, serializerProvider.handlePrimaryContextualization(jsonSerializerFindSerializerFromAnnotation, std), this.findPropertyTypeSerializer(type, serializerProvider.getConfig(), annotatedMember), (type.isContainerType() || type.isReferenceType()) ? this.findPropertyContentTypeSerializer(type, serializerProvider.getConfig(), annotatedMember) : null, annotatedMember, z);
    }

    protected JsonSerializer<?> _findUnsupportedTypeSerializer(final SerializerProvider serializerProvider, final JavaType javaType, final BeanDescription beanDescription) throws JsonMappingException {
        final String strCheckUnsupportedType = BeanUtil.checkUnsupportedType(javaType);
        if (null == strCheckUnsupportedType || null != serializerProvider.getConfig().findMixInClassFor(javaType.getRawClass())) {
            return null;
        }
        return new UnsupportedTypeSerializer(javaType, strCheckUnsupportedType);
    }
}
