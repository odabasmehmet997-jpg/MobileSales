package okhttp3;

import kotlin.jvm.internal.Intrinsics;
import okio.ByteString;

import java.nio.charset.Charset;
import java.nio.charset.StandardCharsets;

import static kotlin.jvm.internal.Intrinsics.checkNotNullExpressionValue;
import static kotlin.jvm.internal.Intrinsics.stringPlus;

// Class generated by  decompiler (1.5.3)
/* compiled from: Credentials.kt */
/* loaded from: D:\Android\sil\Yeni klasör (2)\classes3.dex */
public final class Credentials {
    public static final Credentials INSTANCE = new Credentials();

    private Credentials() {
    }

    public static String basic(final String username, final String password) {
        Intrinsics.checkNotNullParameter(username, "username");
        Intrinsics.checkNotNullParameter(password, "password");
        return Credentials.basicdefault(username, password, null, 4, null);
    }

    public static String basicdefault(final String str, final String str2, Charset ISO_8859_1, final int i2, final Object obj) {
        if (0 != (i2 & 4)) {
            ISO_8859_1 = StandardCharsets.ISO_8859_1;
            checkNotNullExpressionValue(ISO_8859_1, "ISO_8859_1");
        }
        return Credentials.basic(str, str2, ISO_8859_1);
    }

    public static String basic(final String username, final String password, final Charset charset) {
        Intrinsics.checkNotNullParameter(username, "username");
        Intrinsics.checkNotNullParameter(password, "password");
        Intrinsics.checkNotNullParameter(charset, "charset");
        return stringPlus("Basic ", ByteString.Companion.encodeString(username + ':' + password, charset).base64());
    }
}
