package com.fasterxml.jackson.databind.util;

import java.io.IOException;
import java.io.InputStream;
import java.nio.ByteBuffer;

// Class generated by  decompiler (1.5.3)
/* loaded from: D:\Android\sil\Yeni klasör (2)\classes.dex */
public class ByteBufferBackedInputStream extends InputStream {

    /* renamed from: _b */
    protected final ByteBuffer f804_b;

    public ByteBufferBackedInputStream(final ByteBuffer byteBuffer) {
        f804_b = byteBuffer;
    }

    @Override // java.io.InputStream
    public int available() {
        return f804_b.remaining();
    }

    @Override // java.io.InputStream
    public int read() throws IOException {
        if (f804_b.hasRemaining()) {
            return f804_b.get() & 255;
        }
        return -1;
    }

    @Override // java.io.InputStream
    public int read(final byte[] bArr, final int i2, final int i3) throws IOException {
        if (!f804_b.hasRemaining()) {
            return -1;
        }
        final int iMin = Math.min(i3, f804_b.remaining());
        f804_b.get(bArr, i2, iMin);
        return iMin;
    }
}
