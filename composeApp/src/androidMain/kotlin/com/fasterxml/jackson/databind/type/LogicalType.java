package com.fasterxml.jackson.databind.type;

import java.util.Collection;
import java.util.Map;

// Class generated by  decompiler (1.5.3)
/* loaded from: D:\Android\sil\Yeni klasör (2)\classes.dex */
public enum LogicalType {
    Array,
    Collection,
    Map,
    POJO,
    Untyped,
    Integer,
    Float,
    Boolean,
    Enum,
    Textual,
    Binary,
    DateTime,
    OtherScalar;

    public static LogicalType fromClass(final Class<?> cls, final LogicalType logicalType) {
        if (cls.isEnum()) {
            return LogicalType.Enum;
        }
        if (cls.isArray()) {
            if (byte[].class == cls) {
                return LogicalType.Binary;
            }
            return LogicalType.Array;
        }
        if (Collection.class.isAssignableFrom(cls)) {
            return LogicalType.Collection;
        }
        if (Map.class.isAssignableFrom(cls)) {
            return LogicalType.Map;
        }
        return String.class == cls ? LogicalType.Textual : logicalType;
    }
}
